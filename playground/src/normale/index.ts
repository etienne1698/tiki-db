/**
 * README 👋
 * 
 * `normale` is a lightweight data normalization library for Javascript/Typescript applications, with great Typescript support.
 * TODO: it should be extendable for data validation with extensions like `normale-zod` or `normale-arktype`
 *
 */

export { Field } from "./schema/field";
export { schema } from "./schema/schema";
export { string } from "./schema/string";
